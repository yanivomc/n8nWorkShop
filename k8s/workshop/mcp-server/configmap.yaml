apiVersion: v1
kind: ConfigMap
metadata:
  name: mcp-config
  namespace: workshop
data:
  PROMETHEUS_URL: "INJECT_PROMETHEUS_URL"
  # kubeconfig is mounted from a secret
  KUBECONFIG: "/root/.kube/config"
  # TOTP_SECRET and WRITE_APPROVAL_TOKEN injected from secret
