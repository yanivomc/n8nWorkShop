apiVersion: v1
kind: Service
metadata:
  name: event-watcher
  namespace: clawops
spec:
  selector:
    app: event-watcher
  ports:
    - name: http
      port: 8002
      targetPort: 8002
  type: ClusterIP
